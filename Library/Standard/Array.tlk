
struct Array {
	var _storage: Pointer
	var count: int
	var capacity: int

	init() {
		self.count = 0
		self.capacity = 4
		self._storage = allocate(4)
	}

	func append(item) {
		if self.count + 1 > self.capacity	{
			self.resize()
		} else 	{
			self.count = self.count
		}
	}

	func at(index) {

	}

	func resize() {
		newCapacity = self.capacity * 2
		newStorage = allocate(newCapacity)

	}
}
