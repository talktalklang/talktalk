class Array {
	// elements: InternalArray
	init(elements, count) {
		self.elements = elements
		self.count = count
	}

	func subscript(index) {
		return self.elements[index]
	}

	var description {
		return _describe(self.elements)
	}
}
